<van-nav-bar title="重置密码" left-arrow bind:click-left="onBackTap" />

<scroll-view class="password-scroll" scroll-y type="list">
  <view class="password-page">
    <view class="hero-card">
      <view class="hero-badge">RESET</view>
      <view class="hero-title">重置密码</view>
      <view class="hero-desc">通过用户名和手机号验证身份后设置新密码。</view>
    </view>

    <view class="form-card">
      <view class="field-row">
        <van-icon class-prefix="van-icon" name="contact-o" custom-class="field-icon" />
        <input
          class="field-input"
          data-field="username"
          value="{{username}}"
          bindinput="onInput"
          placeholder="请输入用户名"
          placeholder-class="password-placeholder"
        />
      </view>

      <view class="field-row">
        <van-icon class-prefix="van-icon" name="phone-o" custom-class="field-icon" />
        <input
          class="field-input"
          data-field="phone"
          value="{{phone}}"
          bindinput="onInput"
          type="number"
          maxlength="11"
          placeholder="请输入注册手机号"
          placeholder-class="password-placeholder"
        />
      </view>

      <view class="field-row">
        <van-icon class-prefix="van-icon" name="lock" custom-class="field-icon" />
        <input
          class="field-input"
          data-field="newPassword"
          value="{{newPassword}}"
          bindinput="onInput"
          password="{{!showNewPassword}}"
          placeholder="请输入新密码"
          placeholder-class="password-placeholder"
        />
        <button class="eye-btn" data-target="showNewPassword" bindtap="onTogglePassword">
          <van-icon class-prefix="van-icon" name="{{showNewPassword ? 'closed-eye' : 'eye-o'}}" custom-class="eye-icon" />
        </button>
      </view>

      <view class="field-row">
        <van-icon class-prefix="van-icon" name="passed" custom-class="field-icon" />
        <input
          class="field-input"
          data-field="confirmPassword"
          value="{{confirmPassword}}"
          bindinput="onInput"
          password="{{!showConfirmPassword}}"
          placeholder="请再次输入新密码"
          placeholder-class="password-placeholder"
        />
        <button class="eye-btn" data-target="showConfirmPassword" bindtap="onTogglePassword">
          <van-icon class-prefix="van-icon" name="{{showConfirmPassword ? 'closed-eye' : 'eye-o'}}" custom-class="eye-icon" />
        </button>
      </view>

      <button class="primary-btn" loading="{{loading}}" disabled="{{loading}}" bindtap="onSubmitTap">
        <text>确认重置</text>
        <text class="btn-arrow">→</text>
      </button>
    </view>

    <view class="tips-card">
      <view class="tips-head">
        <van-icon name="info-o" custom-class="tips-icon" />
        <text class="tips-title">安全提示</text>
      </view>
      <view class="tips-text">请确保您是账号的合法持有者。重置密码后请妥善保管，不要将密码告知他人。</view>
    </view>
  </view>
</scroll-view>

<van-toast id="van-toast" />
<custom-modal id="custom-modal" />
