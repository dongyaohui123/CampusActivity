package com.campus.activity.enums;

/** Activity status values in activities.status. */
public enum ActivityStatus {
    DRAFT,
    PUBLISHED,
    REGISTRATION_OPEN,
    REGISTRATION_CLOSED,
    ONGOING,
    FINISHED,
    CANCELLED
}
