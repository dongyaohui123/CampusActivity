package com.campus.activity.enums;

/** Registration status values in activity_registrations.status. */
public enum RegistrationStatus {
    REGISTERED,
    CANCELLED,
    WAITLISTED,
    CHECKED_IN
}
